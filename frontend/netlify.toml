# Build settings
[build]
  command = "npm run build"
  publish = "dist"

# Environment settings for all contexts
[environment]
  NODE_VERSION = "18"

# Redirect rule for single-page applications (must be singular "redirect")
[[redirect]]
  from = "/*"
  to = "/index.html"
  status = 200